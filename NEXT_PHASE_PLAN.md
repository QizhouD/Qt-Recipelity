# Recipelity 下一阶段执行计划（MySQL 优先）

> 适用对象：AI 智能体与项目维护者。执行顺序固定为 N0 → N4；先确定产品功能和前端页面，再完成 MySQL、联调与部署，图像识别最后实施。每次只处理一个可验收任务，不得覆盖 `data/recipes.db`。

## 当前判断

Web 重构主体已建立，但尚不满足生产发布条件：

- 图像识别仅有 `NoOpProvider`，`openai` 与 `clip` 分支仍是 TODO。
- 前端没有图片上传、结果校正和生成菜谱草稿页面。
- 后端声明生产使用 Alembic，但仓库中没有 Alembic 配置或迁移版本。
- 前端配置了 Vitest，却没有测试文件；也没有 Playwright 端到端测试。
- Compose 目前仅使用 SQLite，尚未接入目标数据库 MySQL，也未验证完整容器启动。
- 新旧文档和部分 Python 字符串仍出现 `鈥?` 等乱码。

## N0：确定产品范围、页面和交互（P0）

### 首期功能范围

- 菜谱：列表、分页、搜索、筛选、详情、新增、编辑、删除。
- 食材与步骤：结构化增删、排序、数量/单位校验。
- 营养：总量与每份展示、图表、手动录入或重新计算、估算说明。
- 标签与菜系：筛选和菜谱编辑时复用。
- AI 创作：上传食物图片生成菜谱与营养估算；输入菜谱文字生成配图。
- 系统状态：加载、空数据、网络错误、404 和服务不可用页面。
- 图像识别入口暂不展示，待 N4 能力完成后再开放。

### 页面与路由

| 路由 | 页面 | 核心交互 |
|---|---|---|
| `/recipes` | 菜谱首页 | 搜索、筛选、排序、分页、卡片/列表 |
| `/recipes/new` | 新增菜谱 | 基本信息、食材、步骤、标签、营养 |
| `/recipes/:id` | 菜谱详情 | 图片、用料、步骤、营养图表、编辑/删除 |
| `/recipes/:id/edit` | 编辑菜谱 | 数据回填、脏表单离开确认 |
| `/ai-studio` | AI 创作 | 图片生成菜谱、菜谱文字生成配图 |
| `/about` | 关于/说明 | 数据来源、营养免责声明、版本信息 |
| `/:pathMatch(.*)*` | 404 | 返回菜谱首页 |

页面采用响应式布局：桌面端左侧筛选、右侧内容；移动端筛选使用抽屉。筛选条件同步 URL，刷新后保持。删除、覆盖编辑和离开未保存表单必须二次确认。

### 本阶段交付

1. 在 `docs/` 输出功能清单、页面信息架构、页面状态表和 API 字段映射。
2. 统一 Element Plus 设计变量、导航、间距、颜色、按钮和反馈规范。
3. 修复 UTF-8 乱码并更新 README，如实标注图像识别尚未启用。
4. 用静态数据完成所有页面骨架，经确认后再调整 API，避免边开发边改变交互。

验收：所有路由可访问；桌面端和 360px 移动端结构完整；每页均有加载、空、错误和成功状态定义；产品范围不包含隐式需求。

## N1：完成前端功能与契约测试（P0）

1. 固化 OpenAPI，并从契约生成或维护唯一前端 TypeScript 类型。
2. 完成菜谱 CRUD、筛选分页、AI 草稿确认和营养图表的真实 API 联调。
3. 增加 Axios 统一错误处理、请求取消、防重复提交和 401/404/500 展示。
4. 补齐 Vitest，覆盖筛选、表单校验、食材/步骤编辑、Pinia 状态和错误处理。
5. 新增 Playwright，覆盖“筛选 → 详情”“新增 → 编辑 → 删除”“AI 草稿 → 修改 → 保存”。

验收门槛：

```bash
cd backend && ruff check . && black --check . && mypy app && pytest
cd frontend && npm run lint && npm run test:unit && npm run build
docker compose config
```

产物：确定版页面、前端测试报告、OpenAPI/TypeScript 字段映射和更新后的 README。

## N2：MySQL 数据库迁移（P0）

1. 使用 MySQL 8.4 LTS；数据库和连接统一 `utf8mb4`，时区统一 UTC。
2. 后端采用 SQLAlchemy async + `asyncmy`（或经验证的 `aiomysql`），连接信息只读环境变量。
3. 新增 Alembic 初始化迁移；生产启动执行 `alembic upgrade head`，禁止依赖 `create_all`。
4. Compose 新增 MySQL、健康检查、命名数据卷和非 root 应用账户；SQLite 仅保留为旧数据来源和轻量测试选项。
5. 编写 SQLite → MySQL 的 dry-run/正式迁移工具，迁移前备份，迁移后核对表数、行数、关联、时间和中文内容；重复执行不得产生重复记录。
6. 针对 MySQL 执行 CRUD、筛选、事务回滚、唯一约束和 Alembic upgrade/downgrade 集成测试。

验收：全新 MySQL 可从零迁移到 head；旧数据可完整导入；应用重启后数据保留；中文、emoji 和时间字段无损；数据库不可用时 readiness 返回非 2xx。

## N3：发布工程与质量门禁（P1）

1. 实际执行 `docker compose up --build`，验证 MySQL 健康检查、Nginx `/api` 代理、SPA 刷新、数据卷和重启恢复。
2. CI 增加 MySQL 服务、依赖缓存、覆盖率阈值、迁移检查、Playwright 和镜像漏洞扫描。
3. 增加请求 ID、结构化日志和敏感字段脱敏。
4. 完成 AI 上传安全测试：损坏文件、伪造 MIME、超大图片、超时和未配置密钥。
5. 固化生产 `.env.example`、备份/恢复说明、升级与回滚手册。

发布门槛：后端核心新增代码覆盖率 ≥ 80%；关键 E2E 全绿；无高危镜像漏洞；全新环境按 README 一次启动成功并完成 MySQL 初始化。

## N4：生成式 AI 能力（P2）

1. 实现真实 `AIRecipeProvider`，通过环境变量配置密钥、视觉模型、图片模型和超时；强制结构化输出。
2. 验证真实图片格式、像素和大小；低置信度必须提示，失败时不得伪造结果。
3. 新增 `/ai-studio` 页面：图片生成菜谱及营养估算、人工校正；菜谱文字生成配图。
4. 使用 20–50 张授权图片记录 Top-3 命中率、食材召回率、P95 耗时和调用成本。
5. Provider、API 和完整前端流程测试通过后，才在主导航开放入口。

验收：固定图片结果可复现；未配置 Provider 时安全降级；走通“上传 → 校正 → 草稿 → 保存”，且未经用户确认不会创建菜谱。

## AI 智能体任务模板

每个任务开始前输出：

```text
任务：N?.?
目标：
影响文件：
数据/安全风险：
验证命令：
```

完成时必须报告实际命令与结果、未解决问题及回滚方法。数据模型或 API 变化必须同步更新 Alembic、Pydantic、OpenAPI、前端类型和测试。外部服务调用必须有超时、有限重试、日志脱敏和可替换 Provider；任何不可逆迁移、付费服务选择或产品口径变化均须暂停并请求确认。

## 建议立即执行的前三个任务

1. **N0.1：功能与页面规格**——输出页面状态表、字段映射和移动端交互规范。
2. **N0.2：页面骨架评审**——补齐 About、404、全局错误和移动端筛选，暂不显示图像识别。
3. **N1.1：核心前端闭环**——先完成菜谱 CRUD、筛选、AI 草稿和营养展示的测试与联调。
